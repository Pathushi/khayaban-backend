'use strict';

/**
 * menu-fulfillment-channel controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::menu-fulfillment-channel.menu-fulfillment-channel');
