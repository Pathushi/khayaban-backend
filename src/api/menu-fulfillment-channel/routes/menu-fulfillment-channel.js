'use strict';

/**
 * menu-fulfillment-channel router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::menu-fulfillment-channel.menu-fulfillment-channel');
