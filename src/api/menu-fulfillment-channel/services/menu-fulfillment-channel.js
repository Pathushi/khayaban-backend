'use strict';

/**
 * menu-fulfillment-channel service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::menu-fulfillment-channel.menu-fulfillment-channel');
